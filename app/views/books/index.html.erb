<div class="container-fluid">
	<div class="row sampleRow">
		<div class="col-xs-4">

			
		</div>
<div class="col-xs-8">
			<div class="bookdetail">
				<p>Books</p>
				<table class="table table-hover">
					<tr>
						<th></th>
						<th>Title</th>
						<th>Opinion</th>
					</tr>
					<% @books.each do |book| %>
					<tr>
						<td><%= attachment_image_tag book.user, :profile_image, :fill, 40, 40, format: 'jpg',  fallback:"no_image.jpg", size:'40x40' %></td>
						<td><%= book.title %></td>
						<td><%= book.body %></td>
					</tr>
					<% end %>
				</table>
			</div>